export const COMMON = Object.freeze({
  get: 'detailCommon',
});

export const INTRO = Object.freeze({
  get: 'detailIntro',
});

export const INFO = Object.freeze({
  get: 'detailInfo',
});

export const IMAGE = Object.freeze({
  get: 'detailImage',
});

export const PETTOUR = Object.freeze({
  get: 'detailPetTour',
});

export const KEYWORD = Object.freeze({
  get: 'searchKeyword',
});

export const AREABASED = Object.freeze({
  get: 'areaBasedList',
});

export const AREACODE = Object.freeze({
  get: 'areaCode',
});

export const CATEGORY = Object.freeze({
  get: 'categoryCode',
});
export const LOCATION = Object.freeze({
  get: 'locationBasedList',
});
