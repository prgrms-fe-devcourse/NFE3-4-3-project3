const BOOKS = {
  bookList: (searchKeyword: string, searchType: string, selectedKeyword: string) => [
    'bookList',
    searchKeyword,
    searchType,
    selectedKeyword,
  ],
}

const QUERY_KEY = {
  BOOKS,
}

export default QUERY_KEY
