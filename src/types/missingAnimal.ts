export default interface MissingAnimalDataType {
  name: string;
  phone: string;
  species: string;
  age: string; // 나이는 숫자일 수도 있지만 문자열로 제공된 경우에 맞춤
  furColor: string;
  RFID: string;
  feature: string;
  animalType: string;
  gender: string;
  date: string; // 날짜는 문자열로 표현
  placeFeature: string;
  region: string;
  address: string;
}
